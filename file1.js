const log = console.log;

// Start by running this file in your terminal.  You can do that by typing 'node file1'
// This shoud run the javascript file called 'file1.js' and log the following lines.

log("=====================================================");
log("The process object is available to us in node");
log(process);
log("=====================================================");
log(
  "As you can see there are many properties of this object that have information relevant to your system and the current node process running as this code executes"
);

// Take a moment to go over what was logged in the console.
// Proceed to file2
